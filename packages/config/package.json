{
  "name": "@omnihealth/config",
  "version": "1.0.0",
  "description": "Shared configuration loader for OmniHealth services — environment-based, type-safe, with secrets integration",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "echo 'No tests yet'",
    "lint": "echo 'No linter configured'"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "files": ["dist", "src"],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}