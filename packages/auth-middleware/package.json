{
  "name": "@omnihealth/auth-middleware",
  "version": "1.0.0",
  "description": "Express/Koa middleware for JWT validation, scope enforcement, and RBAC/ABAC authorization for OmniHealth services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "echo 'No tests yet'",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {
    "@omnihealth/fhir-types": "1.0.0",
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.1.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.6"
  },
  "peerDependencies": {
    "express": "^4.18.0"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "files": ["dist", "src"],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}